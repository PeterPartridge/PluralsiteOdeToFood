@using PluralsiteOdeToFood.Models
@model RestrauntEditModel

    @{
        ViewBag.Title = "All Restaurants";
    }

<h1>Create a Restaurant</h1>

<div>
    <form method="post">
        <div>
            <span asp-validation-summery></span>
        </div>
        <div>
        <label asp-for="Name"></label>
        <input asp-for="Name" placeholder="Add a name" />
            <span asp-validation-for="Name"></span>
        </div>
        <div>
            <label asp-for="Cusine"></label>
            <select asp-for="Cusine" asp-items="@Html.GetEnumSelectList<CusineType>()"></select>
            <span asp-validation-for="Cusine"></span>
        </div>

        <input type="submit" name="save" value="Save" />

    </form>
</div>